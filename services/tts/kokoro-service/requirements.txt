# Kokoro ONNX runtime backend
kokoro-onnx>=0.5.0
soundfile>=0.12.1

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Numerics
numpy>=1.24.0

# GPU (ROCm/CUDA): install the ROCm build of ONNX Runtime into the venv.
# The plain kokoro-onnx dependency pulls in CPU onnxruntime, so replace it with ROCm:
#   python3 -m venv .venv --system-site-packages
#   .venv/bin/python -m pip uninstall -y onnxruntime
#   .venv/bin/python -m pip install onnxruntime-rocm==1.22.2.post1
