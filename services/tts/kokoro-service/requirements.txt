# Official Kokoro TTS (hexgrad/kokoro) — PyTorch backend
# Auto-downloads model from HuggingFace on first use (~350 MB)
kokoro>=0.9.4
soundfile>=0.12.1

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Numerics
numpy>=1.24.0

# GPU (ROCm/CUDA): use the system PyTorch — create venv with:
#   python3 -m venv .venv --system-site-packages
# This inherits the system torch+rocm installation.
# System torch 2.5.1+rocm6.2 is used; torch.cuda.is_available() returns True on ROCm.
