# Qwen3-TTS runtime
qwen-tts>=0.1.1

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Audio + numeric helpers
numpy>=1.24.0

# Optional GPU memory optimization on supported hardware:
#   pip install flash-attn --no-build-isolation
# ROCm support for qwen-tts is best-effort and must be validated via smoke_test.py.
